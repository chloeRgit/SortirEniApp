{% extends 'base.html.twig' %}



{% block body %}
<div class="row">
    <table class="table table-striped table-hover">
        <thead>
        <tr>
            <th scope="col">Nom de la sortie</th>
            <th scope="col">Date de la sortie</th>
            <th scope="col">Clôture</th>
            <th scope="col">Inscrits/places</th>
            <th scope="col">Etat</th>
            <th scope="col">Inscrit</th>
            <th scope="col">Organisateur</th>
            <th scope="col">Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for s in sorties %}

        <tr>
            <td>{{ s.nom }}</td>
            <td>{{ s.dateHeureDebut |date("d/m/Y H:m")}}</td>
            <td>{{ s.dateLimiteInscription |date("d/m/Y")}}</td>
            <td>{{ s.inscription|length}} / {{ s.nbInscriptionsMax}}  </td>
            <td>{{ s.etat.libelle }}</td>
            <td>inscrit</td>
            <td>{{ s.organisateur.nom }}</td>
            <td>action</td>
        </tr>
        {% endfor %}
        </tbody>
    </table>
</div>


<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/wamp64/www/symfony/SortirEniApp/src/Controller/MainController.php'|file_link(0) }}">src/Controller/MainController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/wamp64/www/symfony/SortirEniApp/templates/main/index.html.twig'|file_link(0) }}">templates/main/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
